/**
 * Barrel for the current-wire Memos social router, which now lives split by
 * domain under `routes/social/`. The import path and the exported instance are
 * unchanged for existing importers.
 */
export * from "./social/index";
