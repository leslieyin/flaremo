/**
 * Barrel for the admin page's two feature cards, which now live under
 * `./admin/`. Kept so existing import paths (the account page) stay valid.
 */

export { BrandingCard } from "./admin/branding-card";
export { AdminPanel } from "./admin/users-card";
